import React from 'react';
import TextFieldsIcon from '@material-ui/icons/TextFields';

export default (props) => {
    return (
        <TextFieldsIcon fontSize={props.size} color="primary"/>
    )
}